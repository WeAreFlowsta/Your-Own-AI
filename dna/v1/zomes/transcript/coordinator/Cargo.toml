[package]
name = "transcript_coordinator"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
hdk = "0.6"
transcript_integrity = { path = "../integrity" }
serde = { version = "1.0", features = ["derive"] }
